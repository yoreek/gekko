{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yoreek.github.io/gekko/schemas/rest/v1/bundle/device-spi_bus.schema.json",
  "title": "spi_bus device-setup record",
  "type": "object",
  "properties": {
    "kind": { "const": "device" },
    "record": {
      "type": "object",
      "properties": {
        "id": { "type": "integer", "minimum": 1 },
        "typeName": { "const": "spi_bus" },
        "configVersion": { "type": "integer", "minimum": 1 }
      },
      "required": ["id", "typeName"],
      "additionalProperties": true
    },
    "config": { "$ref": "../devices/spi_bus.config.schema.json" }
  },
  "required": ["kind", "record", "config"],
  "additionalProperties": false
}
