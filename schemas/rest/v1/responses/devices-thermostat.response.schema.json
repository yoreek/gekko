{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yoreek.github.io/gekko/schemas/rest/v1/responses/devices-thermostat.response.schema.json",
  "title": "thermostat device response",
  "type": "object",
  "properties": {
    "record": {
      "type": "object",
      "properties": {
        "id": { "type": "integer", "minimum": 1 },
        "typeName": { "const": "thermostat" },
        "configRevision": { "type": "integer", "minimum": 0 }
      },
      "required": ["id", "typeName", "configRevision"],
      "additionalProperties": false
    },
    "config": { "$ref": "../devices/thermostat.config.schema.json" },
    "runtime": {
      "type": "object",
      "properties": {
        "status": {
          "enum": ["creating", "starting", "ready", "disabled", "faulted", "dependency_blocked", "reconfiguring", "stopping", "deleting", "unknown"]
        },
        "effectiveStatus": {
          "enum": ["creating", "starting", "ready", "disabled", "faulted", "dependency_blocked", "reconfiguring", "stopping", "deleting", "unknown"]
        },
        "output": {
          "type": "object",
          "properties": {
            "temperature": {
              "type": "object",
              "properties": {
                "value": { "type": "number" },
                "unit": { "enum": ["celsius", "fahrenheit"] },
                "unitSymbol": { "enum": ["C", "F"] },
                "measuredAtMs": { "type": "integer", "minimum": 0 },
                "valid": { "type": "boolean" },
                "status": { "type": "string" }
              },
              "required": ["value", "unit", "unitSymbol", "measuredAtMs", "valid", "status"],
              "additionalProperties": false
            },
            "desiredSwitchState": { "type": "boolean" },
            "actualSwitchState": { "type": "boolean" },
            "lastCheckAtMs": { "type": "integer", "minimum": 0 },
            "controlStatus": { "type": "string" }
          },
          "required": ["temperature", "desiredSwitchState", "actualSwitchState", "lastCheckAtMs", "controlStatus"],
          "additionalProperties": false
        }
      },
      "required": ["status", "effectiveStatus", "output"],
      "additionalProperties": false
    }
  },
  "required": ["record", "config", "runtime"],
  "additionalProperties": false
}
