#!/usr/bin/env sh
# All builds and tests (native unit tests, per-chip firmware, SPA, webflash
# bundles) now run in CI (see .github/workflows/build.yml). data/ and
# webflash/*.bin are no longer git-tracked build outputs -- CI produces them
# as workflow artifacts / release assets / GitHub Pages deploys instead.
# This hook is intentionally a no-op; `git config core.hooksPath .githooks`
# can be left in place or unset, it makes no difference now.
exit 0
